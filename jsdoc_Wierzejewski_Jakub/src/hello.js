/** 
 * Funkcja wyświetlająca w okienku wiadomość dla użytkownika.
 * @param {string} somebody - imię użytkownika.
 * @returns {undefined} - funkcja nie zwraca żadnej wartości.
 * @author Jakub Wierzejewski 5D
*/

const sayHello = (somebody) => {
    alert(`Hello ${somebody}`);
};